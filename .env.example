# Gemini API key used only server-side by POST /api/chat
GEMINI_API_KEY=
# Smithery toolbox URL, e.g. https://mcp.smithery.ai/<toolbox>
SMITHERY_MCP_URL=
# Smithery API key, sent as "Authorization: Bearer". Optional if the URL
# already carries ?api_key=<key>.
SMITHERY_API_KEY=
